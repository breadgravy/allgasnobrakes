#a = !b + v(1) * arr[1];
#func(a,v);
#b = {
#    a = b;
#    ret i : a and b or c to 90 cmp 100;
#}
#a = { 
#    b = d; 
#    n = a + b; 
#    function(sdf,df);
#};  

for i : a to b {
    for i : a to b {
        a + b;
        call_this(then_this({}));
    }
    c = d;
    h + b * gatorade , thethis;
    a = { 
    }
} 
a = b;
func(b,c,d);
a(b);
